version: '3.8'

services:
  angular:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "4200:4200"   # Mapea el puerto 4200
    volumes:
      - ./:/app        # Monta el código fuente para hot-reload
      - /app/node_modules  # Evita sobrescribir node_modules